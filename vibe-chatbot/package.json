{
  "name": "vibe-chatbot",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm -w @vibe/api run dev",
    "start:api": "npm -w @vibe/api start",
    "dev:widget": "npm -w @vibe/widget run dev",
    "build:widget": "npm -w @vibe/widget run build",
    "lint": "echo \"(todo)\"",
    "test": "echo \"(todo)\""
  }
}

